var express = require("express");
var router = express.Router();

users.get("/", (req, res) => {
    res.json({
        
    })
});

users.post("/", (req, res) => {
    res.json({
        
    })
});

module.exports = users;
